{
  "name": "klofan",
  "version": "1.0.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@klofan/eslint-config": "*",
    "@klofan/typescript-config": "*",
    "prettier": "^3.1.1",
    "turbo": "^1.11.3"
  }
}
