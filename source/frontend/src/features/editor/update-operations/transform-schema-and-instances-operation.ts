import { Transformation } from '../../../core/transform/transformation';

export type TransformSchemaAndInstances = {
    type: 'transform-schema-and-instances';
    transformation: Transformation;
};
